# is-even

This library was generated with [Nx](https://nx.dev).

nx build is-even builds the src files and places a ready-to-be-published package in dist/packages/is-even at the root of your workspace

nx publish is-even runs a publish script from dist/packages/is-even to push your package to NPM

nx test is-even runs the pre-configured Jest tests for the package

nx lint is-even runs the pre-configured ESLint checks for the package
